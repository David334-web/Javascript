/*⚠️ NE MODIFIEZ PAS LE NOM DES DÉCLARATIONS ⚠️*/

/*2️⃣ EXERCICE 2️⃣*/

function returnString(string) {
   // Must return a string.
   // Your code:
   return string;
}
console.log(returnString('Merlyn'));

// ⛔️ "X" and "Y" are numbers.

function sum(x, y) {
   // Return the result of their sum.
   // Your code:
   return x + y
}
console.log(sum(12, 23));


function sub(x, y) {
   // Return the result of their subtraction.
   // Your code:
   return x - y
}
console.log(sub(12, 5));


function divide(x, y) {
   // Return the result of your division.
   // Your code:
   return x / y
}
console.log(divide(100, 20));


function multiply(x, y) {
   // Return the result of your multiplication.
   // Your code:
   return x * y
}
console.log(multiply(22, 3))


function getRest(x, y) {
   //Find the remainder of the division of "x" by "y".
   // Your code:
   return x / y
}
console.log(getRest(84, 4))
 